<Window x:Class="OOP_Practice_9.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:uc="clr-namespace:OOP_Practice_9.UserControls"
        mc:Ignorable="d"
        Width="1080" Height="720"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        x:Name="mainWindow">

    <Border Background="#FFFFFF"
            Padding="30"
            CornerRadius="25"
            MouseDown="Border_MouseDown"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border CornerRadius="10" Background="#475272">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="1" BlurRadius="20" Opacity="0.4"/>
                </Border.Effect>
                <Grid>
                    <StackPanel>
                        <Button Style="{StaticResource menuButton}"
                                Margin="15 40 15 20"
                                Background="White">
                            <Button.Triggers>
                                <EventTrigger RoutedEvent="Button.Click">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="page1"
                                                             Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.Y)"
                                                             To="0" Duration="0:0:0.5"/>
                                            <DoubleAnimation Storyboard.TargetName="page2"
                                                             Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.Y)"
                                                             To="{Binding ElementName=mainWindow, Path=ActualHeight}"
                                                             Duration="0:0:0.3"/>
                                            <DoubleAnimation Storyboard.TargetName="page1"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="1.0" Duration="0:0:0.5"/>
                                            <DoubleAnimation Storyboard.TargetName="page2"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0.0" Duration="0:0:0.5"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Button.Triggers>
                            <materialDesign:PackIcon Kind="Home" Foreground="#475272" Height="25" Width="25"/>
                        </Button>

                        <Button Style="{StaticResource menuButton}">
                            <Button.Triggers>
                                <EventTrigger RoutedEvent="Button.Click">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="page1"
                                                             Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.Y)"
                                                             To="{Binding ElementName=mainWindow, Path=ActualHeight}"
                                                             Duration="0:0:0.3"/>
                                            <DoubleAnimation Storyboard.TargetName="page2"
                                                             Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.Y)"
                                                             To="0" Duration="0:0:0.5"/>
                                            <DoubleAnimation Storyboard.TargetName="page1"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0.0" Duration="0:0:0.5"/>
                                            <DoubleAnimation Storyboard.TargetName="page2"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="1.0" Duration="0:0:0.5"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Button.Triggers>
                            <materialDesign:PackIcon Kind="Bed" Foreground="White" Height="25" Width="25"/>
                        </Button>

                        <Button Style="{StaticResource menuButton}">
                            <materialDesign:PackIcon Kind="AccountMultiple" Foreground="White" Height="25" Width="25"/>
                        </Button>
                        <Button Style="{StaticResource menuButton}">
                            <materialDesign:PackIcon Kind="Shield" Foreground="White" Height="25" Width="25"/>
                        </Button>
                        <Button Style="{StaticResource menuButton}">
                            <materialDesign:PackIcon Kind="MapMarker" Foreground="White" Height="25" Width="25"/>
                        </Button>
                        <Button Style="{StaticResource menuButton}">
                            <materialDesign:PackIcon Kind="ChartArc" Foreground="White" Height="25" Width="25"/>
                        </Button>
                        <Button Style="{StaticResource menuButton}">
                            <materialDesign:PackIcon Kind="Cog" Foreground="White" Height="25" Width="25"/>
                        </Button>
                    </StackPanel>

                    <Button Style="{StaticResource menuButton}"
                            VerticalAlignment="Bottom"
                            Margin="15 0 15 25">
                        <materialDesign:PackIcon Kind="Power" Foreground="White" Height="25" Width="25"/>
                    </Button>
                </Grid>
            </Border>

            <Border x:Name="page1" Style="{StaticResource borderPage}">
                <Border.RenderTransform>
                    <TranslateTransform Y="0"/>
                </Border.RenderTransform>
                <Border CornerRadius="25">
                    <Border.Background>
                        <ImageBrush Stretch="Fill" Opacity="0.6" ImageSource="Images/rp1.jpg"/>
                    </Border.Background>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center" Margin="60 0 0 0">
                            <TextBlock Text="Управление"      Foreground="#FFFFFF" FontSize="22"/>
                            <TextBlock Text="Спальня"         Foreground="#FFFFFF" FontSize="36" Margin="0 10" FontWeight="Bold" TextWrapping="Wrap"/>
                            <TextBlock Text="Практическая работа № 9" FontSize="32" Foreground="#FFFFFF"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0 0 50 0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <uc:CardUserControl Grid.Row="0" Grid.Column="0" Margin="0 0 8 8"
                                                    Title="Холодильник"
                                                    ImageOff="Images/roff.png" ImageOn="Images/ron.png"/>
                                <uc:CardUserControl Grid.Row="0" Grid.Column="1" Margin="8 0 0 8"
                                                    Title="Кондиционер"
                                                    ImageOff="Images/koff.png" ImageOn="Images/kon.png"
                                                    IsChecked="True"/>
                                <uc:CardUserControl Grid.Row="1" Grid.Column="0" Margin="0 8 8 0"
                                                    Title="Плита"
                                                    ImageOff="Images/soff.png" ImageOn="Images/son.png"
                                                    IsChecked="True"/>
                                <uc:CardUserControl Grid.Row="1" Grid.Column="1" Margin="8 8 0 0"
                                                    Title="Свет"
                                                    ImageOff="Images/loff.png" ImageOn="Images/lon.png"/>
                            </Grid>

                            <uc:AddButtonUserControl Title="Подключить"
                                                     Margin="0 50 0 0"
                                                     HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Border>

            <Border x:Name="page2" Style="{StaticResource borderPage}" Opacity="0">
                <Border.RenderTransform>
                    <TranslateTransform Y="{Binding ElementName=mainWindow, Path=ActualHeight}"/>
                </Border.RenderTransform>
                <Border CornerRadius="25">
                    <Border.Background>
                        <ImageBrush Stretch="Fill" Opacity="0.6" ImageSource="Images/p1.jpg"/>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <uc:AddButtonUserControl Title="Подключить"
                                                 Margin="0 40 40 0"
                                                 HorizontalAlignment="Right"
                                                 VerticalAlignment="Top"/>

                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 40 0 0">
                            <TextBlock Text="Управление"           Foreground="#FFFFFF" FontSize="22"/>
                            <TextBlock Text="Кухня и Гостиная"     Foreground="#FFFFFF" FontSize="36" Margin="0 10" FontWeight="Bold" TextWrapping="Wrap"/>
                            <TextBlock Text="Практическая работа № 9" FontSize="32" Foreground="#FFFFFF"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" HorizontalAlignment="Left" Margin="30 0 0 15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <uc:CardUserControl Grid.Row="0" Grid.Column="0" Margin="0 0 8 8"
                                                    Title="Холодильник"
                                                    ImageOff="Images/roff.png" ImageOn="Images/ron.png"/>
                                <uc:CardUserControl Grid.Row="0" Grid.Column="1" Margin="8 0 0 8"
                                                    Title="Кондиционер"
                                                    ImageOff="Images/koff.png" ImageOn="Images/kon.png"
                                                    IsChecked="True"/>
                                <uc:CardUserControl Grid.Row="1" Grid.Column="0" Margin="0 8 8 0"
                                                    Title="Плита"
                                                    ImageOff="Images/soff.png" ImageOn="Images/son.png"
                                                    IsChecked="True"/>
                                <uc:CardUserControl Grid.Row="1" Grid.Column="1" Margin="8 8 0 0"
                                                    Title="Свет"
                                                    ImageOff="Images/loff.png" ImageOn="Images/lon.png"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
            </Border>

        </Grid>
    </Border>
</Window>
