<UserControl x:Class="OOP_Practice_9.UserControls.CardUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="cardUC"
             d:DesignHeight="150" d:DesignWidth="160">

    <UserControl.Resources>
        <Style x:Key="localToggle" TargetType="ToggleButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="#4A7572"
                                    CornerRadius="13"
                                    Width="50" Height="26"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"/>
                            <Ellipse x:Name="El"
                                     Fill="#FBFBFB"
                                     Height="22" Width="22"
                                     Margin="25 0 0 0"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="Bd"
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="#9391AB" Duration="0:0:0.2"/>
                                            <ThicknessAnimation Storyboard.TargetName="El"
                                                                Storyboard.TargetProperty="Margin"
                                                                To="1 0 0 0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="Bd"
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="#4A7572" Duration="0:0:0.2"/>
                                            <ThicknessAnimation Storyboard.TargetName="El"
                                                                Storyboard.TargetProperty="Margin"
                                                                To="25 0 0 0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border CornerRadius="25" Padding="15" Height="150">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="1" BlurRadius="20" Opacity="0.5"/>
        </Border.Effect>
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Width"      Value="150"/>
                <Setter Property="Background" Value="#FFFFFF"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ElementName=toggle, Path=IsChecked}" Value="True">
                        <Setter Property="Background" Value="#475272"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding ElementName=cardUC, Path=IsHorizontal}" Value="True">
                        <Setter Property="Width" Value="190"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <TextBlock FontWeight="SemiBold"
                       VerticalAlignment="Top"
                       HorizontalAlignment="Left"
                       Margin="0 4 0 0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text"       Value="Off"/>
                        <Setter Property="Foreground" Value="#475272"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=toggle, Path=IsChecked}" Value="True">
                                <Setter Property="Text"       Value="On"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ToggleButton x:Name="toggle"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Top"
                          Style="{StaticResource localToggle}"
                          IsChecked="{Binding ElementName=cardUC, Path=IsChecked, Mode=TwoWay}"/>

            <Image Width="40" Height="40"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left">
                <Image.Style>
                    <Style TargetType="Image">
                        <Setter Property="Source" Value="{Binding ElementName=cardUC, Path=ImageOff}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=toggle, Path=IsChecked}" Value="True">
                                <Setter Property="Source" Value="{Binding ElementName=cardUC, Path=ImageOn}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>

            <TextBlock VerticalAlignment="Bottom"
                       FontWeight="SemiBold"
                       Text="{Binding ElementName=cardUC, Path=Title}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#2F2C37"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=toggle, Path=IsChecked}" Value="True">
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
